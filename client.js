
// change 'wsAddress' to your WebSocket URL
let wsAddress = ''
const clientSocket = new WebSocket(wsAddress)

clientSocket.addEventListener('open', () => { })
clientSocket.addEventListener('message', () => { })
clientSocket.addEventListener('error', () => { })
clientSocket.addEventListener('close', () => { })




